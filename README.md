# SkyFlare
SkyFlare is a cloud service with similar workflow as cloudflare.

API controller for SkyGuard.

## Getting started

This project requires Go to be installed. On OS X with Homebrew you can just run `brew install go`.
